#include <stdio.h>

/**
 * main - smile in a mirror
 *
 * Return: Always 0 (success)
*/
int main(void)

{
	char low;

	for (low = 'z'; low >= 'a'; low -)
	putchar(low)
	putchar('\n');

	return (0);

}
